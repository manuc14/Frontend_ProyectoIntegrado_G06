export const environment = {
  production: false,
  baseApiUrl: '/api', // dev proxy base para APIs
  baseResourceUrl: '/resources', // dev proxy para recursos estáticos
  useMocks: true, // ⚠️ Cambiar a false cuando backend esté 100% conectado
};
